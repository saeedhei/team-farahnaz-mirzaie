# Pull Request Challenge & Solution Report (Task Cars)

## Problem Description
* During the Pull Request creation, due to matching branch selections (main to main) or spaces in the branch name along with untracked system files, automatic merging was not possible, resulting in the Can’t automatically merge error.

## Solutions Applied
* Used Git terminal commands to create a new branch without spaces (task_cars_new) and pushed it again (git push).
* Managed additional files and fixed untracked folders in the repository.
* Reviewed and finalized changes, successfully opening Pull Request #5.

## Successful Commands Executed
```bash
git checkout -b task_cars_new
git push origin task_cars_new ´´´